package security

/**
import (
	"gopkg.in/mgo.v2/bson"
	"log"
	"time"
)

func CanTrustUser(deps Deps, id bson.ObjectId) (score int, trust bool) {
	var user struct {
		Email     string    `bson:"email"`
		Validated string    `bson:"validated"`
		Created   time.Time `bson:"created_at"`
	}

	return
}
*/
